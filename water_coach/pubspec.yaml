name: water_coach
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' # Adjusted to a common current range

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2 # A common package for iOS style icons
  shared_preferences: ^2.2.0 # Added shared_preferences (example version)
  speech_to_text: ^6.3.0 # Added speech_to_text
  permission_handler: ^11.0.1 # Added permission_handler
  flutter_tts: ^3.8.3 # Added flutter_tts
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0 # Recommended lints

flutter:
  uses-material-design: true
